// Domain entity types, shared across features.
// Sub-entities added in later sub-tasks: story, photo, storyboard-page, scene, sentence,
// voice-profile, style-preset, bgm-preset, person, story-progress.
export {}
